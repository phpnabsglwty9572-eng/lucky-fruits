Lucky Fruits 切图说明（请按此补切后发我替换）
================================

Figma：https://www.figma.com/design/98Af9LIRgS2NAUISXgbI8N/Untitled?node-id=99-2
画板：375 × 667，仅有游戏页 frame「1」(node 99:2)。

当前工程用法
- 已按 Figma Slice 坐标从整屏 4x PNG 精切，并写入 Assets.xcassets。
- Home / Settings / SPIN：圆形透明底；BET / 标题 / 金币条 / 滚轮：圆角透明底。
- 九个符号从滚轮格子裁出（黑底，用于旋转覆盖）。
- 加载页、主页没有独立画板，用游戏页素材 + 金红灯牌风格拼出。

说明：稿里只有一张整图「图-1 1」+ Slice 框，Slice 单独导出是空白，所以不能从 Figma 得到真正分层透明件。

【必须重切】Figma Slice 导出是空白（约几百字节白图）
请在 Figma 里确认 Slice 真正包住图层，用 PNG、2x、透明底 再导出：

1. Slice 主页按钮  99:4   建议 51×51 @1x / 102×102 @2x  透明圆钮
2. Slice 设置按钮  99:5   同上
3. Slice 金币条    99:6   136×51，只要牌面，不要写死「1000」数字
4. Slice 标题      99:7   LUCKY FRUITS 金条，透明底
5. Slice 滚轮框    99:8   336×323，只要金框+灯+空格子（不要水果）
6. Slice BET 10    99:9
7. Slice BET 50    99:10
8. Slice MAX BET   99:11
9. Slice SPIN      99:12  圆形 SPIN，透明底，灯珠完整

【稿里没有、建议补切】
A. 加载页整屏（375×667）或独立 LOGO + 进度条素材
B. 主页整屏（375×667），含 PLAY/开始 而不是 SPIN
C. 九个符号独立 PNG（樱桃/柠檬/橙/葡萄/西瓜/铃/星/BAR/7），透明底、不含邻格金线
D. 空格子黑底（单个 cell）
E. 中奖弹窗 / YOU WIN 牌
F. 按钮按下态（SPIN / BET 凹陷或更亮）
G. 金币条纯数字区或无数字底板
H. 背景层：只要赌场灯光背景，不要叠加 UI（方便三页复用）

【已用、可暂不重切】
- 整屏游戏图（screen@2x）作为游戏页底图
- 从底图裁出的 Home / Settings / BET / MAX / SPIN / 标题 / 金币条
- 九个符号裁图（能玩，但四周会带一点格子金线）

补切后请保持文件名或按上面编号发我，我会直接替换 Assets.xcassets。
